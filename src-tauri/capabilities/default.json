{
  "$schema": "https://schema.tauri.app/capability/2.0.0",
  "identifier": "default",
  "description": "Default permissions for the webgen-motion main window — minimal set, scoped to the shell plugin we need for spawning sidecars (Node server, ffmpeg, etc).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:default",
    "shell:allow-open"
  ]
}
